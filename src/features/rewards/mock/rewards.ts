import { type RawReward } from "../types"

export const rawMockRewards: RawReward[] = [
  {
    id: "reward-free-appetizer",
    title: "Free appetizer",
    description: "Read-only reward for the protected catalogue.",
    pointsRequired: 100,
    isActive: true,
  },
]
